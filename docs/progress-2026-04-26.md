# Aria Underlay 开发进度汇报 2026-04-26

本文记录 2026-04-26 截止当前的整体开发进度、事务性合规状态、今日完成事项、已知风险和下一步计划。

## 1. 当前总体结论

`aria-underlay` 当前方向没有偏离原计划。项目已经从“方案设计”进入“可验证的事务骨架 + Python Adapter fail-closed 边界 + CI 覆盖”的阶段。

当前主线仍然是：

```text
Rust Underlay Core
  -> gRPC / Protobuf
  -> Python Underlay Adapter
  -> NETCONF / Mock Backend
  -> Switch Management Endpoint
```

架构边界保持清晰：

- Rust 负责产品模型、设备纳管、intent、planner、diff、事务状态机、journal、recovery、InDoubt、telemetry event。
- Python Adapter 负责设备连接、NETCONF/NAPALM/CLI 后端、厂商 renderer、设备侧 prepare/commit/verify/recover。
- 单 management endpoint 是当前 ACID 事务边界。
- 多交换机场景按多个 endpoint 独立事务处理，不再宣传跨设备强原子性。

## 2. 当前阶段定位

当前处于 Sprint 1 后段到 Sprint 2 前段的交界：

- Sprint 0 capability probe 已形成可运行路径。
- Sprint 1A mock adapter、事务状态、失败路径、journal phase 覆盖已经比较完整。
- Sprint 1B real NETCONF backend 的准备工作已经开始，真实设备联调前的 fail-closed 骨架已落地。
- Sprint 2 renderer skeleton 已启动，但 Huawei/H3C renderer 仍明确标记为 skeleton，不能进入生产下发路径。

## 3. 今日重点完成事项

### 3.1 Adapter/gRPC 测试公共化

已完成：

- 新增 Rust 测试公共 fake adapter helper。
- 复用公共 fake adapter server，减少重复测试服务实现。
- 覆盖 adapter 连接失败、recover outcome、transaction gate、telemetry 等路径。

价值：

- 后续添加 gRPC 异常路径测试时成本更低。
- 测试数据和 adapter 行为更统一。
- 减少不同测试文件之间 fake 行为不一致导致的误判。

相关提交：

- `4dfd908 Share adapter test server helper`

### 3.2 真实 NETCONF prepare 的 fail-closed 边界

已完成：

- `NcclientNetconfBackend.prepare_candidate()` 已接入真实设备侧 prepare 骨架：
  - `lock(candidate)`
  - render candidate XML
  - `edit-config(candidate)`
  - `validate(candidate)`
  - 失败后 `discard-changes`
  - 最后 `unlock(candidate)`
- renderer 缺失时返回结构化错误，不会进入假成功。
- skeleton renderer 被真实 backend 拒绝，不允许下发未验证 XML。

价值：

- 没有真实交换机时，也能先把“不能假成功、不能乱下发”的边界锁住。
- 真实设备到位后，只需要补 vendor XML 和设备差异，不需要重写事务边界。

相关提交：

- `0e6ad35 Gate skeleton renderers from production prepare`

### 3.3 Renderer registry fail-closed

已完成：

- 新增 `adapter-python/aria_underlay_adapter/renderers/registry.py`。
- 默认拒绝 skeleton renderer。
- 未注册厂商返回 `RENDERER_VENDOR_UNSUPPORTED`。
- Huawei/H3C skeleton 默认返回 `RENDERER_NOT_PRODUCTION_READY`。
- 只有测试或离线 snapshot 显式传入 `allow_skeleton=True` 才能使用 skeleton renderer。
- `NetconfBackedDriver.prepare()` 在真正触碰设备锁之前完成 renderer 选择和生产可用性检查。
- capability probe 不被 renderer gate 阻断，避免设备纳管阶段误伤。

价值：

- 解决“renderer skeleton 误入生产路径”的风险。
- 进一步满足 Fail-closed 红线。
- 为 Sprint 2 真实 renderer 开发建立清晰准入标准。

相关提交：

- `505a5d3 Add fail-closed renderer registry`

### 3.4 Journal / shadow 写入失败处理收紧

已完成：

- journal 写入失败不再静默忽略。
- shadow 写入失败不再被普通 warning 掩盖。
- 事务结果可以明确体现 durability 相关错误。

价值：

- Durability 语义更严谨。
- 避免“设备已经变更，但本地事务记录丢失”这类事故被误判为成功。

相关提交：

- `d71a4d4 fix: stop silently dropping journal and shadow write failures`

### 3.5 安全和校验缺陷修复

已完成：

- endpoint lock 指数退避中的 jitter 字段已实际生效。
- VLAN ID 边界校验已补齐，非法 VLAN 不再进入 adapter 映射。
- rollback artifact path traversal 已阻断。
- renderer 对 `access_vlan = None` 不再输出字面量 `"None"`。

价值：

- 减少锁冲突抖动。
- 防止非法配置进入南向。
- 修复 rollback artifact 存储安全风险。
- 避免生成错误 XML。

相关提交：

- `3c5c7d3 fix: jitter, VLAN validation, path traversal, and XML None guard`

### 3.6 Bug inventory 固化

已完成：

- 新增 2026-04-26 全量代码审查 bug inventory。
- 按 Rust/Python、严重程度、模块位置归类。
- 标记已修复项和仍待处理项。
- 后续 P0/P1 排期直接从该清单中抽取。

价值：

- 今天的缺陷盘点不会散落在聊天记录里。
- 后续开发可以持续对照“已修复 / 未修复 / 风险等级”推进。
- 便于阶段复盘时判断是否真正收紧了正确性边界。

相关提交：

- `c4b67f9 docs: add bug inventory from 2026-04-26 codebase review`

### 3.7 Mock full-scope verify 缺口关闭

已完成：

- `mock_netconf` 在 `scope.full = true` 或未传 scope 时，不再把 verify 范围错误当成空集合。
- full/default verify 会检查 observed running 中的 VLAN/interface，能够发现 desired 之外的额外资源。
- 新增 full/default scope 的 VLAN/interface 异常路径测试。

价值：

- 测试后端不再出现 full-scope verify 假成功。
- 真实 verify/parser 尚未完成前，mock 集成测试可以更严格地暴露收敛问题。

相关提交：

- `7a765f0 fix: close mock full-scope verify gap`

### 3.8 Intent validation 收紧

已完成：

- `SwitchPairIntent` 增加 pair_id、switch ID、VLAN、interface、access/trunk VLAN 引用校验。
- `UnderlayDomainIntent` 增加 domain_id、management endpoint、secret_ref、member 引用、topology shape 校验。
- 拒绝重复 endpoint/member/switch/VLAN/interface。
- 拒绝 VLAN 0、4095 以及声明外 VLAN 引用。
- domain planner 在生成 `DeviceDesiredState` 前统一调用 shared validation。

价值：

- 非法 intent 会在 Rust API/planner 边界 fail-closed，不再晚到 adapter 或设备侧才失败。
- 进一步满足 ACID 中 Consistency 的输入约束。
- 对“产品初始化时录入管理 IP、NETCONF 用户/密码后开始初始化”的纳管路径更安全：空 host、空 secret_ref、拓扑与管理 IP 数量不匹配都会提前失败。

相关提交：

- `33572f8 fix: strengthen intent validation`
- `4b78632 fix: align intent validation errors`

### 3.9 Journal / artifact GC 最小可用版本

已完成：

- `JournalGc::run_once()` 不再是空实现。
- 按 `RetentionPolicy` 清理旧的 terminal journal：
  - `Committed`
  - `RolledBack`
  - `Failed`
- `InDoubt` 和非终态事务不会被自动 GC。
- rollback artifact 只在能从 journal 确认关联事务是 terminal 时清理。
- 支持 `max_artifacts_per_device`，按设备保留最新 terminal artifact。
- 新增 GC 单元测试覆盖：
  - 旧 terminal journal 删除。
  - InDoubt journal 保留。
  - terminal artifact 删除。
  - unknown tx artifact 保留。
  - 单设备 artifact 数量上限。

价值：

- Durability 不再只有“写入”，也有明确留存和清理策略。
- 避免长期私有化部署后 journal/artifact 无限制增长。
- 不牺牲 recovery 安全性：无法证明 terminal 的事务和 artifact 不会被 GC。

### 3.10 Renderer hardening

已完成：

- 新增共享 `StructuredSkeletonRenderer` 和 `RendererNamespaceProfile`。
- Huawei/H3C renderer 不再复制同一套 VLAN/interface 渲染逻辑，而是通过 namespace profile 区分。
- VLAN delete 使用 NETCONF base namespace 下的 `operation="delete"`。
- renderer 层增加 fail-closed 输入校验：
  - VLAN ID 必须在 1..4094。
  - interface name 不能为空。
  - trunk 不能没有 native VLAN 且没有 allowed VLAN。
  - trunk allowed VLAN 不能重复。
  - unknown port mode 继续拒绝。
- renderer 测试增加 profile、NETCONF namespaced operation、非法 VLAN、空接口、非法 trunk 等覆盖。

价值：

- Sprint 2 的 renderer 边界更清晰，后续切真实 Huawei/H3C namespace 时只需要替换 profile 和字段映射。
- 继续保持“没有真实验证就不 production_ready”的 fail-closed 原则。
- Python adapter 即使被绕过 Rust intent validation 直接调用，也会在 renderer 层再次拒绝明显非法配置。

### 3.11 Journal GC worker telemetry

已完成：

- 新增 `JournalGcWorker::run_once_and_emit()` 单次执行入口。
- GC 成功后会通过 `EventSink` 发出 `UnderlayJournalGcCompleted` 事件。
- GC telemetry event 会记录：
  - `journals_deleted`
  - `journals_retained`
  - `artifacts_deleted`
- GC 失败时错误继续返回，不发成功事件。

未完成：

- 还没有接入长期运行的周期调度 loop。
- 还没有补磁盘配额、审计落库和运行时 shutdown 策略。

价值：

- `JournalGc::run_once()` 不再只是可被测试的清理函数，已经有 worker-facing 的可观测执行入口。
- 后续接后台调度时，可以复用同一个单次执行边界，避免把清理逻辑和事件上报逻辑散落到服务层。

### 3.12 真实 NETCONF scoped state filter 骨架

已完成：

- `NcclientNetconfBackend.get_current_state(scope)` 已接入 scoped running 读取路径。
- 空 scope 返回空状态并跳过设备读取，避免没有变更范围时产生无意义 RPC。
- full scope 执行 `get-config(source="running")`，但真实 parser 未完成前仍返回 `NETCONF_STATE_PARSE_NOT_IMPLEMENTED`。
- 非空 scoped scope 会构造 subtree filter，并执行 `get-config(source="running", filter=("subtree", xml))`。
- `verify_running(desired_state, scope)` 复用同一 scoped running 读取路径；读取后仍返回 `NETCONF_VERIFY_NOT_IMPLEMENTED`，不会假装验证成功。
- `StateScope.vlan_ids` 会排序、去重并校验 VLAN 范围。
- `StateScope.interface_names` 会排序、去重、XML escape，并拒绝空接口名。

关键测试：

- full scope 不带 filter 读取 running。
- 空 scope 不触碰设备。
- scoped VLAN/interface filter 会排序、去重、转义。
- 非法 VLAN scope 返回 `INVALID_STATE_SCOPE`。
- parser/verify 未实现时仍显式 fail-closed，不返回假状态或假成功。

价值：

- 真实设备到位前，已经固定 Rust -> adapter -> NETCONF `get-config` 的 scope 边界。
- 后续只需要补厂商 running state parser 和 vendor-specific filter/profile，不需要重做 gRPC 或事务调用链。
- touched subtree refresh / post-commit scoped verify 的性能优化有了协议层落点，同时不牺牲正确性。

### 3.13 真实 NETCONF running state parser 注入边界

已完成：

- `NcclientNetconfBackend` 新增 `state_parser` 注入点。
- 未配置 parser 时，`get_current_state` 和 `verify_running` 仍 fail-closed，不返回假状态。
- 非 production-ready parser 会返回 `NETCONF_STATE_PARSER_NOT_PRODUCTION_READY`。
- production-ready parser 可以把 `get-config` 返回的 XML 转换为标准 observed state。
- `verify_running` 在 parser 可用时会用解析后的 observed state 对比 desired state，覆盖 VLAN、interface、description、admin state、access/trunk mode。
- parser 返回结构会做基础 shape 校验，避免非 dict / 缺少字段导致静默假成功。

价值：

- 真实设备 parser 还没写之前，生产路径继续关闭。
- 下一步实现 Huawei/H3C parser 时，只需要实现 `parse_running(xml, scope)`，事务层、driver 层和 gRPC 契约不需要重做。
- post-commit verify 已经具备“读取 running -> parser -> desired 对比”的完整骨架。

### 3.14 State parser registry 与厂商 skeleton

已完成：

- 新增 `state_parsers` 模块。
- Huawei/H3C running state parser skeleton 已登记。
- parser registry 默认拒绝 skeleton parser，返回 `STATE_PARSER_NOT_PRODUCTION_READY`。
- Cisco/Ruijie/Unknown 等未登记厂商返回 `STATE_PARSER_VENDOR_UNSUPPORTED`。
- `NetconfBackedDriver.get_current_state()` 和 `verify()` 在触碰设备前先选择 production-ready parser。
- 如果 parser 未验证，真实 NETCONF backend 不会先读设备再失败，避免产生“状态读取已成功”的误判。

价值：

- state parser 和 renderer 一样进入 fail-closed 准入模式。
- 真实设备 parser 未完成前，不会有假 state、假 verify 或 skeleton parser 进入生产路径。
- 后续接 Huawei/H3C parser 时只需要把对应 parser 标记为 `production_ready = True`，并补真实设备验证记录。

### 3.15 Sprint 2B fixture-verified state parser

已完成：

- 新增 Huawei VRP8 / H3C Comware7 running XML fixture。
- 新增共享 fixture parser helper，覆盖：
  - VLAN ID / name / description。
  - interface name / admin state / description。
  - access VLAN。
  - trunk native VLAN 和 allowed VLAN。
- Huawei/H3C parser 从 skeleton 进入 `fixture_verified=True` 状态。
- registry 默认仍保持 fail-closed，`production_ready=False` 时不会被生产路径选择。
- 测试可显式传入 `allow_fixture_verified=True` 获取 fixture parser。
- fail-closed 覆盖缺失 VLAN ID、非法 VLAN、重复接口和未知 port mode。

未完成：

- 尚未使用真实交换机 `get-config` XML 验证。
- parser 仍不是 production-ready。
- 真实厂商 namespace / YANG 字段路径仍需现场样本确认。

价值：

- 已补上 “running XML -> 标准 observed state” 的最小闭环。
- `get_current_state` / `verify_running` 的 parser 注入边界可以通过 fixture parser 做本地单元验证。
- 继续维持真实设备路径 fail-closed，不把 fixture 结果宣传成生产可用。

### 3.16 Sprint 2C fixture parser driver/backend integration

已完成：

- `NetconfBackedDriver` 新增默认关闭的 `allow_fixture_verified_parser` 显式开关。
- driver 自动注入 state parser 时，只有开启该开关才允许 `fixture_verified=True` parser 通过 registry。
- NETCONF backend 新增默认关闭的 fixture parser 信任边界，生产路径仍要求 `production_ready=True`。
- 新增 driver/backend 集成测试覆盖：
  - Huawei fixture XML -> `GetCurrentState` -> 标准 `ObservedDeviceState`。
  - H3C fixture XML -> `GetCurrentState` 基础路径。
  - Huawei fixture XML -> `Verify` 匹配成功。
  - desired-vs-observed mismatch 返回 `VERIFY_FAILED`。
  - scoped VLAN/interface 只返回 scope 内状态，并下发 scoped subtree filter。
  - empty scope 不读设备。

未完成：

- fixture parser 仍不是 production-ready。
- 真实 Huawei/H3C 设备 XML、namespace、字段路径和异常形态仍未实机验证。
- 真实生产启用前，还需要用现场样本验证 parser profile，并单独决定是否切换 `production_ready=True`。

价值：

- 已把“NETCONF get-config reply -> fixture parser -> observed state -> driver protobuf response / verify diff”的本地闭环跑通。
- production 默认路径仍在读设备前拒绝未生产验证 parser，避免 fixture parser 被误用为真实设备 parser。

### 3.17 Sprint 2D offline state parser validator

已完成：

- 新增 `aria-underlay-state-parse` Python console script。
- validator 可读取本地 NETCONF running XML 文件，并通过 fixture-verified parser 输出标准 observed-state JSON。
- 支持 `--vendor`、`--xml`、`--full`、可重复 `--vlan`、可重复 `--interface`。
- parser / registry 失败会返回非 0，并向 stderr 输出结构化 JSON 错误：
  - unsupported vendor 返回 `STATE_PARSER_VENDOR_UNSUPPORTED`。
  - XML 缺字段等解析错误返回 `NETCONF_STATE_PARSE_FAILED`。
- README 已补充真实样本离线验证用法。

未完成：

- 还没有真实设备 XML 样本落库。
- validator 只是样本资格验证工具，不会改变生产 driver 的 parser 准入策略。
- parser 是否能升级到 `production_ready=True` 仍需要真实 Huawei/H3C 样本和现场行为验证。

价值：

- 真实交换机 XML 一拿到，就可以不用改代码先跑离线 parser 验证。
- 能把真实样本解析失败变成稳定的 JSON 错误，便于记录 profile 差异和补测试。
- 继续保持 fixture-verified 与 production-ready 的边界。

### 3.18 Sprint 2E validator field usability

已完成：

- `aria-underlay-state-parse` 新增 `--pretty`，用于人工检查 observed-state JSON。
- `aria-underlay-state-parse` 新增 `--summary`，用于现场快速确认：
  - vendor。
  - parser profile。
  - `fixture_verified` / `production_ready` 状态。
  - VLAN / interface 解析数量。
  - 当前 scope。
- summary 支持 scoped XML 验证，能快速确认 touched resources 是否被 parser 读到。
- README 已补充 summary 和 pretty 用法。

未完成：

- 仍未接入真实设备 XML 样本库。
- summary 只是现场 triage 输出，不代表 parser 可以生产启用。

价值：

- 现场拿到 XML 后，不需要先阅读完整 JSON，就能判断 parser profile 和资源计数是否合理。
- 继续把样本验证和 production-ready 准入分开处理。

### 3.19 Sprint 2F real NETCONF state sample runbook

已完成：

- 新增 `docs/runbooks/netconf-state-sample-capture.md`。
- 明确 Huawei/H3C 真实 `get-config` running XML 的采集流程。
- 明确 raw capture 必须保存在 git 外，提交前必须脱敏和裁剪。
- 明确脱敏清单：
  - 管理 IP、hostname。
  - 用户名、密码、密钥、SNMP community、AAA、证书。
  - 序列号、MAC、资产编号、站点名。
  - 客户 VLAN 名称、接口描述、公网地址、peer、租户名。
- 新增 `adapter-python/tests/fixtures/state_parsers/real_samples/README.md`。
- 定义真实样本目录结构、metadata 文件模板、validator 命令和 production readiness 准入边界。

未完成：

- 还没有真实 Huawei/H3C XML 样本提交。
- 还没有基于真实样本新增 parser profile test。
- parser 仍保持 `production_ready=False`。

价值：

- 真实交换机到位后，采样、脱敏、验证、沉淀 fixture 的流程已经固定。
- 防止未脱敏 running config 误入仓库。
- 防止单个样本验证成功被误解为生产可用。

### 3.20 Sprint 2G validator manifest batch validation

已完成：

- `aria-underlay-state-parse` 新增 `--manifest`。
- manifest 支持一次性列出多份 Huawei/H3C redacted running XML。
- manifest XML 路径支持绝对路径，也支持相对 manifest 文件位置的相对路径。
- 每个 sample 独立解析，输出统一 batch report：
  - `ok`。
  - `sample_count`。
  - `passed` / `failed`。
  - 每个 sample 的 parser summary 或结构化 error。
- 任一样本失败时命令返回 `1`，但仍输出完整批次结果，方便现场一次性看到所有样本状态。
- manifest shape、scope VLAN/interface 类型错误会 fail-closed，并向 stderr 输出结构化 JSON。
- README 和真实样本目录说明已补充 manifest 用法。

未完成：

- 仍未接入真实 Huawei/H3C XML 样本库。
- manifest 只是离线 triage 和回归入口，不会改变生产 driver 的 parser 准入策略。
- parser 仍保持 `production_ready=False`，需要真实样本和测试闭环后再单独评审。

价值：

- 现场拿到多份 XML 后，可以一次性跑批量验证，不需要逐个手工执行命令。
- 混合成功/失败样本能在同一份 JSON 报告里归档，便于后续补 parser fixture 和回归测试。
- 为真实样本库建立了可重复的批量验收入口。

### 3.21 Sprint 2H offline renderer snapshot validator

已完成：

- 新增 `aria-underlay-render-snapshot` Python console script。
- 支持从 desired-state JSON 离线渲染 Huawei/H3C skeleton edit-config XML。
- 输出 JSON snapshot report：
  - vendor。
  - renderer profile。
  - `production_ready`。
  - VLAN / interface 计数。
  - 渲染后的 XML。
- 支持 `--pretty`，便于人工 review snapshot。
- unsupported vendor、非法 desired-state JSON、空 desired state、非法 VLAN 等路径会 fail-closed，并输出结构化 JSON 错误。
- README 已补充离线 renderer snapshot 用法。

未完成：

- renderer 仍是 skeleton，仍不能生产下发。
- 真实 Huawei/H3C namespace 和字段映射仍需真实设备或厂商 YANG 资料验证。
- 还未建立正式 snapshot fixture 目录和 golden XML 文件。

价值：

- 没有真实交换机时，可以继续把 renderer 输出稳定性、输入边界和 profile 信息管住。
- 后续调整 vendor namespace / field mapping 时，可以先通过离线 snapshot 回归发现 XML 变化。
- 继续保持 production prepare 默认拒绝 skeleton renderer 的安全边界。

## 4. ACID / 红线合规状态

| 红线 | 当前状态 | 说明 |
| --- | --- | --- |
| 幂等性 | 基本合规 | apply 前已有 dry-run/diff/no-op 路径；mock full-scope verify 已补严；后续需继续强化 shadow freshness 和 touched refresh。 |
| Fail-closed | 明显增强 | 未实现 driver、未注册 vendor、skeleton renderer/parser、缺失 renderer/parser、非法 renderer 输入都不会假成功。 |
| Capability 驱动 | 基本合规 | capability probe、策略选择、降级 opt-in 已接入；真实设备 capability 歧义仍需联调验证。 |
| Drift 检测 | 部分合规 | 已有接口和基础路径；后台 auditor 和结构化 drift diff 仍需继续完善。 |
| Recovery 可恢复 | 部分合规 | recover RPC、journal phase、InDoubt 路径已覆盖；journal/artifact GC 最小版本已补；崩溃恢复深水区测试还需继续做。 |
| InDoubt 严格处理 | 基本合规 | InDoubt 会传播到 apply 结果；后续需补人工 ForceResolve API。 |
| 凭据安全 | 基本合规 | 使用 `secret_ref` 和本地 secret provider；日志脱敏仍需持续检查。 |
| 可观测性 | 部分合规 | telemetry event builder 和 event sink 已接入；metrics/audit 还未完整生产化。 |
| 测试优先 | 持续增强 | Rust/Python 单测和 GitHub Actions 集成矩阵已覆盖大量失败路径；真实设备测试待补。 |
| Isolation | 部分合规 | Rust 本地 endpoint lock、退避、超时已接入；设备侧 NETCONF lock 骨架已接入 prepare。 |

## 5. 当前已完成能力概览

| 模块 | 当前进度 | 备注 |
| --- | --- | --- |
| Protobuf 契约 | 已建立 | 包含 capability、state、prepare、commit、verify、recover、force unlock。 |
| Rust API / Service | 已有主流程 | 支持 apply、dry-run、refresh、site init、recovery 等骨架。 |
| Device onboarding | 已有基础路径 | 支持 product initialization 时注册设备并触发 capability probe。 |
| Intent / Planner | 已有 domain 模型演进 | 当前以 management endpoint 为事务边界；intent/domain 输入校验已收紧。 |
| Diff / Normalize | 已有基础实现 | VLAN、interface、trunk allowed VLAN 等路径已覆盖。 |
| Transaction | 已有单 endpoint 编排 | Prepare、commit、verify、final confirm、rollback、InDoubt 等路径逐步完善。 |
| Journal | 已接入 | GC 最小可用版本已补；崩溃恢复深水区测试仍需加强。 |
| Python Adapter | 已有 gRPC server | fake backend、mock NETCONF、real NETCONF skeleton 均存在。 |
| Mock backend | 已有较丰富覆盖 | 支持模拟 lock/edit/validate/commit/verify/recover 失败。 |
| Real NETCONF backend | 骨架继续收紧 | prepare candidate 已 fail-closed；scoped get-config filter、parser 注入边界和 parser registry 已补；Huawei/H3C fixture parser 已通过 driver/backend 本地集成测试，并新增现场可用的离线 XML validator、batch manifest 和真实样本采集 runbook；真实厂商 state parser 和 vendor XML 仍未实机验证。 |
| Renderer | skeleton 阶段 | Huawei/H3C skeleton 已 profile 化并补充输入校验，并新增离线 snapshot validator；只能用于测试/snapshot，不能生产下发。 |
| Telemetry | 初步接入 | event builder 和 sink 已有，metrics/audit 仍需补。 |
| CI | 当前绿色 | GitHub Actions 已验证 Rust、Python、多个集成矩阵。 |

### 5.1 2026-04-30 事务可靠性修复进度

本轮先处理不依赖真实交换机的 ACID/事务可靠性问题，修复方案见：

- [Transaction Reliability Hardening Plan](./superpowers/plans/2026-04-30-transaction-reliability-hardening.md)

已落地：

- `Committed` journal 现在只在 shadow state 成功写入后写入，关闭“journal terminal 但 shadow 未落盘”的主要崩溃窗口。
- shadow 写入失败会把事务标记为 `InDoubt`，不再返回 `SuccessWithWarning`。
- 多设备聚合状态不再把“部分成功 + 部分失败/回滚”报告成成功警告。
- recovery 批量路径在恢复前重新读取 journal 记录，避免使用 list 阶段的过期记录继续恢复。
- journal 增加 `error_history`，保留阶段性错误历史，不再只剩最后一个错误。
- NETCONF prepare 出错后，如果 discard 也失败，会保留原始 prepare 错误并附加 discard 失败细节。
- mock NETCONF 端口模式 normalize 对未知模式 fail-closed，不再静默当成 access。
- ncclient 错误分类去掉裸 `"auth"` 子串匹配，避免把 authorization 文本误判为 authentication failure。

验证状态：

- Python adapter 全量测试已在本地通过：`188 passed`。
- `git diff --check` 已通过。
- 当前 shell 缺少 `cargo`/`rustc`，Rust 本地测试无法执行。
- GitHub Actions `25165739879` 已通过 Rust `cargo check`、Rust `cargo test`、Python adapter tests 和集成矩阵。

### 5.2 2026-04-30 InDoubt 运维闭环进度

本轮继续处理不依赖真实交换机的事务运维闭环，修复方案见：

- [Force Resolve In-Doubt Transactions Plan](./superpowers/plans/2026-04-30-force-resolve-in-doubt-transactions.md)

已落地：

- 新增 `force_resolve_transaction()` Rust service API。
- 新增 `ForceResolveTransactionRequest` / `ForceResolveTransactionResponse`。
- 新增 terminal `TxPhase::ForceResolved`。
- journal 新增 `manual_resolution` 元数据，记录 operator、reason、request_id、trace_id 和 resolved timestamp。
- force resolve 只允许处理当前 `InDoubt` 事务。
- force resolve 必须显式启用 `break_glass_enabled=true`，并提供 operator 和 reason。
- service 在写入前会按 journal 记录中的 devices 获取 endpoint lock，并在锁内重新读取 journal，避免解除过期状态。
- `ForceResolved` 不再进入 recoverable 列表，也不再阻塞后续事务。
- telemetry/audit 新增 `transaction.force_resolved` 事件。

验证状态：

- Python adapter 全量测试已在本地通过：`188 passed`。
- `git diff --check` 已通过。
- 当前 shell 缺少 `cargo`/`rustc`/`rustfmt`，Rust 本地测试和格式化无法执行；Rust 编译和测试需要通过 GitHub Actions 验证。

### 5.3 2026-04-30 事务运维入口进度

本轮继续把 `InDoubt` 从“核心层可解除”推进到“运维可发现、可调用”，实施计划见：

- [Sprint 2I Transaction Ops Entry Plan](./superpowers/plans/2026-04-30-sprint-2i-transaction-ops-entry.md)

已落地：

- 新增 `list_in_doubt_transactions()` Rust service API。
- 新增 `ListInDoubtTransactionsRequest` / `ListInDoubtTransactionsResponse` / `InDoubtTransactionSummary`。
- 查询结果只返回当前 `TxPhase::InDoubt` 的 journal 记录，不返回 `Prepared`、`Committed`、`ForceResolved` 等其它阶段。
- 查询结果包含 tx_id、request_id、trace_id、devices、strategy、当前错误、error_history 和时间戳，便于运维判断是否可以人工解除。
- 新增 `examples/transaction_ops.rs`，可从 JSON journal 目录执行：
  - `list-in-doubt`
  - `force-resolve`
- `force-resolve` example 不直接编辑 journal JSON，而是复用 service 的 `force_resolve_transaction()`，继续保留 break-glass、operator、reason、phase 校验和审计写入。

范围边界：

- 这一步不连接真实交换机。
- 这一步不新增生产 RPC server 或 UI。
- 这一步先提供可编译、可审计的离线运维入口；后续可再把同一 service API 接到正式控制面。

验证状态：

- 本地 Python adapter 全量测试通过：`188 passed`。
- 本地 `git diff --check` 通过。
- 本地 shell 缺少 `cargo`/`rustc`/`rustfmt`。
- GitHub Actions `25167543318` 已通过：Rust `cargo check --all-targets`、Rust `cargo test`、Python adapter、fake-adapter 集成矩阵均为 success。

### 5.4 2026-04-30 Sprint 2J：事务 crash/restart 矩阵进度

本轮继续处理不依赖真实交换机的事务持久性验证，实施计划见：

- [Sprint 2J Transaction Crash/Restart Matrix Plan](./superpowers/plans/2026-04-30-sprint-2j-transaction-crash-restart-matrix.md)

已落地：

- 新增 `JsonFileShadowStateStore`，补齐之前只有 in-memory shadow store 的持久化缺口。
- file shadow store 使用 JSON 文件存储，写入采用 temp file + rename，revision 语义与 in-memory store 对齐。
- 新增 shadow store 重启后读取、跨重启 revision 递增、list 排序、remove、device_id path sanitize 测试。
- 新增 file-backed journal 重启恢复测试：服务重建后仍能扫描 pending record，并按现有 recovery 语义转入 `InDoubt`。
- 新增 file-backed force-resolve 重启测试：`ForceResolved` 记录服务重建后不再进入 recovery，也不会出现在 list-in-doubt 结果里。
- 新增成功 apply 后 file-backed shadow 持久化测试：fake adapter 成功下发后，服务重建仍能读到 desired shadow state。

范围边界：

- 这一步不连接真实交换机。
- 这一步验证的是本地 file-backed journal/shadow 的重启持久性，不证明真实 NETCONF confirmed-commit/persist-id 语义。

验证状态：

- 本地 `git diff --check` 通过。
- 本地 shell 缺少 `cargo`/`rustc`/`rustfmt`，Rust 测试无法本地执行。
- GitHub Actions `25169323213` 已通过：Rust `cargo check --all-targets`、Rust `cargo test`、Python adapter tests 和 fake-adapter 集成矩阵均为 success。

### 5.5 2026-04-30 Sprint 2K：NETCONF dry-run 预检进度

本轮继续处理不依赖真实交换机的 Python adapter dry-run 缺口，实施计划见：

- [Sprint 2K NETCONF Dry-Run Preflight Plan](./superpowers/plans/2026-04-30-sprint-2k-netconf-dry-run-preflight.md)

已落地：

- 将“后续开发优先选择长期合理、架构稳定、现场可运维方案”的原则写入开发方案。
- 新增 backend 级 `CandidateDryRunResult` / `dry_run_candidate()` 契约。
- `NcclientNetconfBackend.dry_run_candidate()` 复用 prepare 的 renderer production gate 和 candidate XML 渲染校验。
- 抽出 `_render_candidate_config()`，避免 dry-run 和 prepare 各维护一套相似但可能分叉的 renderer 校验逻辑。
- real NETCONF dry-run 明确为只读预检：不打开设备 session、不 lock candidate、不 edit-config、不 validate candidate。
- `NetconfBackedDriver.dry_run()` 不再抛 `NotImplementedError`，而是返回结构化 `DryRunResponse`。
- dry-run 对 unsupported vendor、skeleton renderer、renderer failure 均 fail-closed，并返回明确错误码。
- mock backend 新增 dry-run 预览逻辑，使用 `_merge_desired_state()` 判断是否会变化，但不修改 running state。

范围边界：

- 这一步不连接真实交换机。
- 这一步证明的是“adapter 可安全做 renderer 预检”，不证明真实设备会接受该 XML。
- 真实设备到位后，如需设备侧 `validate` 级 dry-run，应作为显式 remote-validate 模式新增，不能混入默认只读 dry-run。

验证状态：

- targeted dry-run tests 通过：`8 passed`。
- Python adapter 全量测试通过：`199 passed`。
- 本地 `git diff --check` 通过。

### 5.6 2026-04-30 Sprint 2L：Secret Orphan Cleanup

关联计划：

- [Sprint 2L Secret Orphan Cleanup Plan](./superpowers/plans/2026-04-30-sprint-2l-secret-orphan-cleanup.md)

已落地：

- `SecretStore::create_for_device` 从单纯返回 `secret_ref` 升级为返回 `SecretProvisioningResult`。
- `SecretProvisioningResult` 明确标记该 secret 是否由当前初始化请求创建，以及注册失败时是否应清理。
- `InMemorySecretStore` 增加 `delete(secret_ref)`，用于初始化失败补偿清理。
- `UnderlaySiteInitializationService` 在 secret 创建成功、device registration 失败时自动删除本次创建的 secret。
- 对 `ExistingSecretRef` 保持只引用不删除，避免误删外部或预置凭据。
- 如果 cleanup 本身失败，初始化响应保留 `secret_ref`，并把 registration error 与 cleanup error 一起返回，方便现场人工处理。

新增测试：

- 注册失败后，本次创建的 secret 会被删除，响应不再暴露已删除的 `secret_ref`。
- cleanup 失败时，响应保留 orphan `secret_ref`，错误信息包含注册失败和 cleanup 失败两段上下文。

范围边界：

- 这一步只处理“注册失败后新建 secret 孤儿”的补偿清理。
- 已成功注册但后续 onboarding 失败的设备仍保留 inventory 与 secret，因为现有语义是 `PartiallyRegistered`，不是站点级全量回滚。
- 后续如果要把 site initialization 做成强事务，需要另起设计，把 inventory、secret、onboarding 状态一起纳入补偿协议。

### 5.7 2026-04-30 Sprint 2M：AdapterClient Channel Pool

关联设计与计划：

- [Adapter Client Pool Design](./superpowers/specs/2026-04-30-adapter-client-pool-design.md)
- [Sprint 2M Adapter Client Pool Plan](./superpowers/plans/2026-04-30-sprint-2m-adapter-client-pool.md)

已落地：

- 新增 `AdapterClientPool`，按 `adapter_endpoint` 缓存 tonic `Channel`。
- `AdapterClient` 增加 `from_channel(Channel)`，pool 每次返回新的 client facade，避免共享 mutable RPC client。
- `AriaUnderlayService` 持有一个 pool，覆盖 dry-run/current-state、apply、recovery、drift audit、refresh 和 force unlock。
- `DeviceOnboardingService` 支持注入 pool。
- `UnderlaySiteInitializationService` 在 site initialization 中把同一个 pool 传给 onboarding。
- 保留 `AdapterClient::connect(endpoint)`，供独立 probe/example 直接使用。
- 新增 `invalidate(endpoint)` 作为后续健康检查、失败策略和运维控制的扩展点。

范围边界：

- 这一步复用 gRPC channel，不实现 idle eviction、per-endpoint max connection、健康检查 worker。
- tonic `Channel` 本身支持 clone、HTTP/2 multiplexing 和 reconnect；当前无需额外对象池。
- 真实设备不可用不影响这一步验证，fake adapter CI 可以覆盖 Rust core 与 Python adapter 的连接生命周期。

### 5.8 2026-04-30 Sprint 2N：Parser Fixture Boundary Expansion

关联计划：

- [Sprint 2N Parser Fixture Boundaries Plan](./superpowers/plans/2026-04-30-sprint-2n-parser-fixture-boundaries.md)

已落地：

- 新增 Huawei/H3C namespace 变体正例 fixture，验证 parser 使用 local-name 匹配，不依赖固定 XML namespace。
- 新增 Huawei/H3C 负例 fixture 目录：`adapter-python/tests/fixtures/state_parsers/negative/`。
- 新增负例覆盖：缺 VLAN ID、重复 VLAN、非法 VLAN、重复接口、空接口名、unknown port mode、trunk 无 native/allowed VLAN、allowed VLAN 重复。
- `tests/test_state_parsers.py` 改为 fixture 驱动验证 Huawei/H3C fail-closed 行为。
- 新增 parser 边界测试后，现有 parser 代码无需修改；测试确认当前 fail-closed 逻辑已覆盖这些边界。

范围边界：

- 这一步提升的是 fixture-verified 可信度，不等于真实设备 production-ready。
- Huawei/H3C parser 仍需要真实 running XML 样本验证 namespace、字段路径、缺省字段和厂商差异。
- Cisco/Ruijie parser 仍未实现，需等待样本或明确厂商 profile。

### 5.9 2026-04-30 ConfirmedCommit Recovery Hardening

计划文档：

- [Confirmed Commit Recovery Hardening Plan](./superpowers/plans/2026-04-30-confirmed-commit-recovery-hardening.md)

本轮修复：

- 更新 `docs/bug-inventory-2026-04-30.md`，区分仍真实存在、已经过期和当前修复中的 bug。
- `TxJournalRecord` 新增 `desired_states` 和 `change_sets`，用于崩溃恢复时证明 touched resources 的最终状态。
- `apply_single_endpoint_state()` 在事务 journal 初始记录中保存 desired state 和 change set。
- `FinalConfirming + ConfirmedCommit` 恢复改为专用路径：
  - 先重试 `final_confirm`；
  - 如果不能证明成功，则用 journal 中的 desired state + change set scope 调用 verify；
  - 如果仍不能证明，再走原有 adapter recover；
  - 全部无法证明时写入 `FINAL_CONFIRM_RECOVERY_IN_DOUBT`，不把不确定状态误标为成功。
- 新增回归测试覆盖“final confirm 已消费 persist-id，但 verify 能证明 desired state 已落地”的恢复路径。

验证边界：

- 本地 Python adapter 全量测试：`217 passed`。
- 本地 `git diff --check` 通过。
- 本地没有 `cargo`/`rustc`，Rust 编译和新增 recovery test 需要以 GitHub Actions 为准。

### 5.10 2026-04-30 HostKeyPolicy Wiring

计划文档：

- [HostKeyPolicy Wiring Plan](./superpowers/plans/2026-04-30-host-key-policy-wiring.md)

本轮修复：

- `DeviceRef` protobuf 新增 `host_key_policy`、`known_hosts_path`、
  `pinned_host_key_fingerprint`。
- Rust `device_ref_from_info()` 开始把 `DeviceInfo.host_key_policy` 映射到
  Python adapter 请求，不再在 Rust/Python 边界丢弃安全策略。
- Python `_netconf_driver_from_device()` 根据 `DeviceRef.host_key_policy`
  配置 `NcclientNetconfBackend`。
- `KnownHostsFile` 通过 `hostkey_verify=True` 和 ncclient SSH config shim
  指向指定 `UserKnownHostsFile`。
- `PinnedKey` 先做到端到端传输和 fail-closed：当前 ncclient 只暴露
  `hostkey_b64` exact-key pinning，而 Rust 模型保存的是 fingerprint；因此
  不做静默降级，打开 session 时返回 `HOST_KEY_PINNING_UNSUPPORTED`。

验证边界：

- `python3 -m pytest -q adapter-python/tests/test_server_renderer_selection.py adapter-python/tests/test_netconf_backend.py::test_ncclient_backend_rejects_pinned_fingerprint_without_exact_pin_support`
  通过。
- Rust mapper 新增 `maps_host_key_policy_to_device_ref` 覆盖；本地没有
  `cargo`/`rustc`，最终编译和测试以 GitHub Actions 为准。

### 5.11 2026-05-01 TrustOnFirstUse Host Key Store

本轮修复：

- `TrustOnFirstUse` 不再只是 strict known-hosts 占位语义。
- Python `NcclientNetconfBackend` 新增 TOFU trust store 路径：
  `ARIA_UNDERLAY_TOFU_KNOWN_HOSTS_FILE`，默认
  `/tmp/aria-underlay-adapter/tofu_known_hosts`。
- 首次连接时使用 `hostkey_verify=False` 读取远端 host key；只有在成功把
  `[host]:port key_type key` 原子写入 trust store 后，才把 session 返回给
  上层流程。
- 后续连接如果 trust store 已存在该 host/port，使用 ncclient strict
  known-hosts 校验。
- 如果远端 host key 无法读取、trust store 写入失败，或并发首次信任发现已有
  不同 key，均 fail-closed。

验证边界：

- 本地新增 pytest 覆盖首次写入、后续 strict 校验、写入失败 fail-closed、
  server policy wiring 和 config env。
- `PinnedKey` 仍然保持 fail-closed，等待后续 fingerprint-only 精确 pinning
  支持。

### 5.12 2026-05-01 P2 Python Adapter Hygiene

本轮修复：

- Python vendor driver stubs 不再在构造时抛 `NotImplementedError`；构造可用，
  实际操作仍 fail-closed。
- NETCONF backend、mock backend、renderer 共享同一个
  `admin_state_to_text` 归一化 helper，避免 unspecified/zero admin state 在
  测试和渲染路径里分叉。
- NETCONF state filter 和 fixture state parser 的 scope VLAN 转换增加
  per-index 错误上下文，非法值返回结构化 `AdapterError`，不再泄漏裸
  `ValueError`。

验证边界：

- 已按 TDD 先确认新增聚焦测试在旧实现下失败，再修实现。
- 本地聚焦 pytest 覆盖 vendor stub、NETCONF scope、mock/render admin state、
  parser scope 五个回归点。
- `a3cc43a` / GitHub Actions run `25197914750` passed。

### 5.13 2026-05-01 P2 Rust Intent/Lock Hardening

本轮修复：

- `SmallFabric` topology endpoint 语义收敛为至少两个 management endpoint，
  不设硬编码上限；单 endpoint 场景应使用 `StackSingleManagementIp`。
- endpoint lock 指数退避 jitter 不再用 wall-clock nanoseconds 取模，而是用
  `rand::thread_rng()` 生成独立 jitter。
- `add_jitter` helper 明确约束 jitter 不超过当前 delay 的 25%。

验证边界：

- 新增 Rust 回归测试覆盖 SmallFabric 单 endpoint 拒绝、SmallFabric 多 endpoint
  无硬上限仍可规划、jitter helper 25% 上界。
- 本地无 `cargo`/`rustfmt`，本包 Rust 编译与测试以 GitHub Actions 为准。
- `9808ef7` / GitHub Actions run `25198141224` passed。

### 5.14 2026-05-01 P2 Rust Renderer Dead-Code Cleanup

本轮修复：

- 删除 Rust `src/device/render.rs` 中未被生产路径调用的 renderer skeleton。
- 移除 `src/device/mod.rs` 对 `renderer_for_vendor`、`DeviceConfigRenderer` 和
  vendor renderer struct 的重导出。
- 删除只覆盖这些死 skeleton 的 Rust renderer tests。

验证边界：

- `rg` 确认 Rust renderer 只被 re-export 和对应测试引用；真实配置渲染仍在
  Python adapter renderer registry。
- 本地无 `cargo`/`rustfmt`，本包 Rust 编译与测试以 GitHub Actions 为准。
- `d286f3b` / GitHub Actions run `25198279834` passed。

### 5.15 2026-05-01 Architecture Hygiene Package 1 — Docs Truth

本轮修复：

- 更新根 `README.md`，移除过期的 Sprint 0 状态描述。
- 明确当前已经具备 Rust transaction core、Python NETCONF backend、
  fail-closed renderer/parser registry、TOFU trust store、dry-run preflight、
  parser validator 和 renderer snapshot validator。
- 明确 Huawei/H3C renderer 仍是 skeleton，Huawei/H3C parser 仍只是
  fixture-verified，不能在真实设备路径冒充生产可用。
- 明确 NAPALM / Netmiko / SSH CLI 后端仍是规划项，不是已实现路径。
- 新增本轮架构卫生执行计划：
  `docs/superpowers/plans/2026-05-01-architecture-hygiene.md`。

验证边界：

- 本包只改文档和计划，不改变运行时代码。
- `git diff --check` 必须通过。
- `c415bad` / GitHub Actions run `25198760941` passed。

### 5.16 2026-05-01 Architecture Hygiene Package 2 — Python Placeholder Cleanup

本轮修复：

- 删除 Python package 中未被生产代码引用的空壳模块：
  - `aria_underlay_adapter.diff`
  - `aria_underlay_adapter.rollback`
  - `aria_underlay_adapter.state`
  - `aria_underlay_adapter.backends.napalm_backend`
  - `aria_underlay_adapter.backends.netmiko_backend`
- 新增包卫生测试，防止这些 `pass` placeholder 再次作为“看起来已实现”的
  import surface 被发布出来。
- 更新 adapter README，明确 NAPALM / Netmiko 未实现且不再发布空壳模块。

验证边界：

- TDD red：新增包卫生测试在删除前失败，证明旧 placeholder 仍可 import。
- 本包不实现 NAPALM / Netmiko；后续真正实现时应新增真实模块和测试，并同步更新
  包卫生测试。
- `f6396f2` / GitHub Actions run `25198892159` passed。

### 5.17 2026-05-01 Architecture Hygiene Package 3 — Rust Service Boundary

本轮修复：

- 从 `src/api/service.rs` 拆出 apply 结果聚合、错误映射、degraded warning、
  commit status 判断等纯 helper 到 `src/api/apply.rs`。
- 从 `src/api/service.rs` 拆出 recovery / final-confirm recovery / in-doubt
  summary / force-resolve validation helper 到 `src/api/recovery_ops.rs`。
- 从 `src/api/service.rs` 拆出 drift policy error 构造到
  `src/api/drift_ops.rs`。
- `AriaUnderlayService` 继续保留 facade、trait impl 和需要访问 service
  字段的编排流程；本包不改变事务行为。
- `service.rs` 从约 1759 行降到约 1373 行，后续可以继续按 apply/recovery
  coordinator 方向拆更深的 orchestration。

验证边界：

- 本地 `python3 -m pytest adapter-python/tests` 通过，`231 passed`。
- 本地 `git diff --check` 通过。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- `a58907b` / GitHub Actions run `25199109107` passed。

### 5.18 2026-05-01 Architecture Hygiene Package 4 — Python NETCONF Boundary

本轮修复：

- 从 `adapter-python/aria_underlay_adapter/backends/netconf.py` 拆出
  ncclient 错误归类到 `netconf_errors.py`。
- 从 `netconf.py` 拆出 KnownHostsFile / TOFU trust store / host key
  持久化 helper 到 `netconf_hostkey.py`。
- 从 `netconf.py` 拆出 running `get-config` filter、running XML 读取、
  state parser gating、desired-vs-observed verify helper 到
  `netconf_state.py`。
- `NcclientNetconfBackend` 继续保留 public backend facade、candidate
  transaction 操作和兼容导出；本包不改变 dry-run、prepare、commit、
  rollback、verify 的行为语义。
- 新增 package hygiene 测试，防止 `netconf.py` 重新退化为单个千行文件。

验证边界：

- TDD red：结构性 hygiene 测试在 helper 模块拆出前失败。
- 本地聚焦 pytest：
  `python3 -m pytest adapter-python/tests/test_package_hygiene.py adapter-python/tests/test_netconf_backend.py -q`
  通过，`59 passed`。
- 本地完整 Python adapter 测试通过：`232 passed`。
- 本地 `git diff --check` 通过。
- GitHub Actions 状态以后续提交记录为准。

### 5.19 2026-05-01 Transaction Crash/Restart Matrix Expansion

本轮修复：

- 在既有 file-backed recovery / force-resolve / shadow persistence 测试基础上，
  继续补齐重启边界矩阵。
- 新增 journal 终态重启测试：`Committed`、`Failed`、`RolledBack`、
  `ForceResolved` 记录重启后仍可读取，但不会进入 `list_recoverable()`。
- 新增 journal 损坏文件测试：恢复扫描遇到 corrupt JSON 必须 fail-closed，
  不允许静默跳过可能影响恢复决策的事务记录。
- 新增 journal `.tmp` 崩溃残留测试：atomic write 遗留的临时文件不参与恢复扫描。
- 新增 shadow 损坏文件测试：重启后读取 corrupt shadow JSON 必须 fail-closed。
- 新增 shadow `.tmp` 崩溃残留测试：临时文件不参与 shadow list。

验证边界：

- 本轮仍不触碰真实交换机、不模拟真实 NETCONF session drop。
- 测试重点是 file-backed journal/shadow 的重启语义、终态过滤、损坏文件
  fail-closed 和 atomic temp residue 处理。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后才能进入下一步。

### 5.20 2026-05-01 Architecture Hygiene Package 5 — Rust Orchestration Coordinators

本轮修复：

- 从 `src/api/service.rs` 继续拆出带流程的事务编排边界，而不只是纯 helper。
- 新增 `src/api/apply_coordinator.rs`：
  - 持有 apply / dry-run / per-endpoint prepare-commit-verify-final-confirm 编排。
  - 保留 shadow 先于 terminal journal 的提交顺序。
  - 保留 InDoubt gate、drift policy gate、rollback-on-failure 和 apply event emission。
- 新增 `src/api/recovery_coordinator.rs`：
  - 持有 recover_pending_transactions 主流程。
  - 持有 FinalConfirming + ConfirmedCommit 的 roll-forward-first recovery 路径。
  - 保留锁内重读 journal 和恢复后重新统计 pending / InDoubt 的行为。
- 新增 `src/api/admin_ops.rs`：
  - 持有 list-in-doubt、force-unlock、force-resolve 等人工/运维入口。
  - 保留 force-resolve 的 break-glass 校验、endpoint lock、锁内重读和审计事件。
- `AriaUnderlayService` 现在主要保留构造器、trait facade、设备状态读取和 drift audit
  入口；service.rs 从约 653 行继续降到约 489 行。

验证边界：

- 本包目标是结构拆分，不改变事务协议语义或 API shape。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后才能进入下一步。

### 5.21 2026-05-01 Ops Audit / Metrics Summary Loop

本轮修复：

- 新增统一的 operator-facing summary 层：
  `src/telemetry/ops.rs`。
  - `OperationSummary::from_event()` 将 force-resolve、drift、GC、recovery、
    transaction InDoubt 事件转成可查询摘要。
  - `InMemoryOperationSummaryStore` 支持记录事件、列出全部摘要、列出
    `attention_required=true` 的运维待处理项。
- 扩展 telemetry event：
  - 新增 `UnderlayDriftAuditCompleted`，drift audit 即使没有漂移也会有完成事件。
  - 新增 `UnderlayRecoveryCompleted`，recovery scan 会输出 recovered / in_doubt /
    pending / tx_count 摘要。
  - 既有 `UnderlayTransactionForceResolved` 和 `UnderlayJournalGcCompleted` 纳入统一
    summary 视图。
- 扩展 metrics：
  - 新增 force-resolve、recovery、recovery-in-doubt、drift-audit、
    drift-detected、journal-gc、journal-gc-deleted 等运维计数。
  - 新增 `Metrics::record_event()`，可以直接从统一事件流生成操作计数。
- service 层补齐事件发射：
  - `run_drift_audit()` 在每次 audit 结束时发 completion event。
  - `recover_pending_transactions()` 在每次 recovery scan 结束时发 completion event。

验证边界：

- 本包不增加外部持久化审计数据库；当前提供内存型 summary store 和事件/metrics
  统一入口。
- 生产环境后续可以把同一 `OperationSummary` 写入外部审计系统或 UI 查询层。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后此 4 步闭环完成。

### 5.22 2026-05-01 Transaction Process Chaos Package 1

本轮修复：

- 新增 process-level chaos integration test：
  `tests/transaction_process_chaos_tests.rs`。
  - 父测试进程分配临时 journal/shadow 目录和固定 adapter endpoint。
  - 子测试进程启动 adapter，执行真实 `apply_domain_intent()`。
  - adapter 在 `final_confirm` RPC 里直接退出进程，模拟服务进程在
    `FinalConfirming` 阶段崩溃，同时 adapter session 一起断开。
  - 父进程重建 file-backed journal/shadow store，验证 journal 停在
    `FinalConfirming`，且 shadow 尚未写入。
- 补两条恢复场景：
  - adapter 在同一 endpoint 重启后，`recover_pending_transactions()` 能把
    `FinalConfirming` roll-forward 到 `Committed`。
  - adapter session 仍不可达时，恢复不会假成功，会把事务推进到
    `InDoubt`，并记录 `FINAL_CONFIRM_RECOVERY_IN_DOUBT`。
- 修复 recovery roll-forward 的 shadow 持久化窗口：
  `src/api/recovery_coordinator.rs` 现在接入 `shadow_store`。
  - 恢复路径确认事务可提交后，先用 journal 内保存的 desired state 补写
    shadow。
  - shadow 写入成功后才允许写 terminal `Committed` journal。
  - 如果恢复提交已确认但 shadow 写入失败，事务保持/进入 `InDoubt`，避免
    “journal 已 Committed 但 shadow 缺失”的崩溃窗口在 recovery 路径复发。

验证边界：

- 本包覆盖 `FinalConfirming` 进程崩溃和 adapter session-drop 两个最高价值场景。
- 尚未覆盖所有阶段的进程级 kill 点，例如 `Preparing`、`Committing` 和多设备混合
  recovery；这些可以在后续 chaos package 中继续扩展。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后才能进入下一包。

### 5.23 2026-05-01 GC Productionization Package 1

本轮修复：

- 扩展 `JournalGcReport` 的 operator-facing 删除摘要：
  - 新增 `journal_deleted_tx_ids`。
  - 新增 `artifact_deleted_refs`，格式为 `device_id/tx_id`。
  - 新增 `deleted_total()`，方便 telemetry/metrics/UI 汇总展示。
- `UnderlayJournalGcCompleted` 事件现在除了删除/保留计数外，还会携带：
  - `deleted_total`
  - `journal_deleted_tx_ids`
  - `artifact_deleted_refs`
- 新增周期调度入口：
  - `JournalGcSchedule`
  - `JournalGcSchedulerReport`
  - `JournalGcWorker::run_periodic_until_shutdown()`
- 调度入口行为：
  - `interval_secs=0` fail-closed，返回明确配置错误。
  - 支持 `run_immediately=true`，便于进程启动后先清一轮。
  - 接受 shutdown future，方便后续挂到 daemon lifecycle，不把 GC 做成无法退出的后台任务。
- 测试补充：
  - GC event 包含删除明细。
  - 旧 terminal journal 删除时记录 tx_id。
  - artifact retention 和 per-device cap 删除时记录 `device/tx`。
  - periodic worker 能立即执行一轮并响应 shutdown。
  - zero-second interval 被拒绝。

验证边界：

- 本包已经提供 production process 可挂载的周期 worker API，但尚未把它接入实际 daemon
  lifecycle；当前仓库还没有统一 runtime/daemon supervisor。
- 持久化审计 sink 和 UI/API 查询面仍属于后续 OperationSummary/query 包。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后才能进入下一包。

### 5.24 2026-05-01 Drift Background Audit Package 1

本轮修复：

- 扩展 `DriftAuditor`：
  - 保留既有 `run_once()` 返回 drift reports 的 API。
  - 新增 `run_once_with_summary()`，返回 `DriftAuditRunSummary`。
  - summary 明确包含 `audited_devices`、`drifted_devices` 和 drift reports。
- 新增 drift 后台 worker：
  - `DriftAuditWorker`
  - `DriftAuditSchedule`
  - `DriftAuditSchedulerReport`
- worker 行为：
  - `run_once_and_emit()` 会为每个 drifted report 发
    `UnderlayDriftDetected`。
  - 每轮 audit 都会发 `UnderlayDriftAuditCompleted`，包括无漂移的 clean run。
  - `run_periodic_until_shutdown()` 支持 `run_immediately=true`，并接受 shutdown
    future，方便后续挂到 daemon lifecycle。
  - `interval_secs=0` fail-closed，返回明确配置错误。
- 测试补充：
  - summary 会统计 clean + drifted 的总审计设备数。
  - worker 会发 drift detected 和 audit completed 事件。
  - periodic worker 能立即执行一轮并响应 shutdown。
  - zero-second interval 被拒绝。

验证边界：

- 本包提供可挂载的后台巡检 loop，但尚未接入真实 daemon supervisor 或告警系统。
- `AutoReconcile` 仍保持 fail-closed；本包只做发现、记录、事件和调度入口。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后才能进入下一包。

### 5.25 2026-05-01 Operation Summary Query Package 1

本轮修复：

- 新增 operator-facing 查询 API 类型：
  `src/api/operations.rs`。
  - `ListOperationSummariesRequest`
  - `ListOperationSummariesResponse`
- `UnderlayService` 新增：
  `list_operation_summaries()`。
- `AriaUnderlayService` 现在内置 `InMemoryOperationSummaryStore`。
  - service 发出的 operator event 会继续进入外部 `EventSink`。
  - 同一事件也会进入 operation summary store。
  - 支持注入自定义 summary store，便于测试或后续持久化替换。
- 新增 `RecordingEventSink`：
  - fan-out 到原有 `EventSink`。
  - 同时把事件转换成 `OperationSummary`。
- 查询能力：
  - 列出全部 operation summaries。
  - 只列出 `attention_required=true` 的摘要。
  - 按 `action`、`device_id`、`tx_id` 过滤。
  - 支持 `limit`，保留最近 N 条。
- 测试补充：
  - drift audit 仍会写入配置的外部 event sink。
  - 同一轮 drift audit 可以通过 `list_operation_summaries()` 查到
    `drift.detected` 和 `drift.audit_completed`。
  - attention-only、action filter 和 limit 均有覆盖。

验证边界：

- 本包提供 service/API 级查询面，但不是持久化审计数据库。
- 生产 UI/CLI 可以基于该 API 接入；长期仍需要把 summary store 替换或镜像到持久化后端。
- 本地没有 `cargo`、`rustc`、`rustfmt`，Rust 编译与测试以 GitHub Actions
  为准。
- 本地需跑 `git diff --check` 和 Python adapter 回归；Rust CI 绿色后此轮 4 包闭环完成。

## 6. 当前 CI 状态

截至本文档更新时，`main` 最近几次 GitHub Actions 状态如下：

- `d286f3b`：`fix: remove dead rust renderers`，success。
- `9808ef7`：`fix: harden rust p2 topology and lock jitter`，success。
- `a3cc43a`：`fix: harden python adapter p2 hygiene`，success。
- `817607d`：`fix: implement tofu host key trust store`，success。
- `3086ee3`：`docs: add development progress report`，success。
- `c4b67f9`：`docs: add bug inventory from 2026-04-26 codebase review`，success。
- `7a765f0`：`fix: close mock full-scope verify gap`，success。
- `24953519003`：`fix: jitter, VLAN validation, path traversal, and XML None guard`，success。
- `24953491511`：`Add fail-closed renderer registry`，success。
- `24953384446`：`fix: stop silently dropping journal and shadow write failures`，success。
- `24953311732`：`Gate skeleton renderers from production prepare`，success。
- `24952488973`：`Share adapter test server helper`，success。

本地当前没有 `cargo`/`rustfmt`，Rust 检查和测试以 GitHub Actions 为准。

## 7. 仍然存在的主要缺口

| 优先级 | 缺口 | 影响 |
| --- | --- | --- |
| P1 | journal/artifact GC 仍需 daemon 集成 | 清理逻辑、retention、单次 worker telemetry、周期调度入口和删除明细已补；后续还需要接入真实 daemon lifecycle、持久审计落库、磁盘配额和真实 artifact store 联调。 |
| P1 | intent validation 需随模型扩展持续维护 | 当前 VLAN/interface/domain 基础校验已补齐；后续 ACL/VRF/route 等新模型必须同步增加校验。 |
| P1 | DriftAuditor 仍需 daemon/alert 集成 | 已有一次性 shadow vs observed source 对比、summary、事件发射和周期 worker 入口；后续还需要接入真实 daemon lifecycle、告警系统和设备状态更新策略。 |
| P1 | real NETCONF 厂商 state parser 未实机验证 | parser 注入边界、fixture parser、driver/backend fixture 集成、现场 validator 和样本采集 runbook 已补；还不能证明华为/H3C 真实 running XML 可稳定解析成标准 ObservedDeviceState。 |
| P1 | vendor renderer 仍是 skeleton | skeleton 边界已收紧；真实华为/H3C 设备还不能安全下发生产 XML。 |
| P1 | crash recovery / session 中断测试仍需加强 | 已补 `FinalConfirming` 进程崩溃、adapter session-drop 和恢复 roll-forward shadow-before-journal 断言；仍需扩展到 `Preparing`、`Committing` 和多设备混合 kill 点。 |
| P1 | host key trust workflow 仍需生产化 | HostKeyPolicy 已跨 Rust/Python 边界传递，KnownHostsFile 和 TOFU 持久信任库可执行；fingerprint-only pinning 的精确实现仍需后续设计。 |
| P2 | metrics/audit 持久化不足 | service 级 operation summary 查询面已补；现场长期审计仍需要持久化 sink、UI/CLI 和权限模型。 |

详细 bug 清单见：

- [Current Bug / Tech Debt Inventory 2026-05-01](./bug-inventory-current-2026-05-01.md)
- [Bug Inventory 2026-04-30](./bug-inventory-2026-04-30.md)
- [Bug Inventory 2026-04-26](./bug-inventory-2026-04-26.md)

## 8. 下一阶段建议

下一阶段继续保持“正确性优先，性能不牺牲正确性”的顺序。

### 8.1 P0：继续收紧正确性

优先做：

- 继续扩展 process-level chaos kill points，或等待真实设备 XML 进入 parser/renderer 验证。
- 继续为 intent validation 增加随模型演进的回归测试。

目标：

- 测试环境不能漏报错误。
- 已完成事务和 artifact 有明确留存策略，并逐步接入后台执行。
- 非法输入在 Rust API/planner 边界尽早失败，并随模型扩展持续保持。

### 8.2 P1：真实设备 XML 样本验证

优先做：

- 从 Huawei/H3C 设备导出原始 `get-config` running XML。
- 用 `aria-underlay-state-parse` 离线验证样本。
- 按 runbook 完成脱敏、裁剪和 metadata 记录。
- 把失败样本按 profile 差异补成最小 fixture。
- 只有样本和测试闭环后，才讨论 parser `production_ready=True`。

目标：

- 真实设备到位后，先验证读状态路径，而不是直接进入下发配置。
- 确认 Huawei/H3C namespace、字段路径和异常形态。

### 8.3 P1：Sprint 2 renderer hardening

优先做：

- 为 Huawei/H3C renderer 建立 namespace profile。
- 明确 skeleton / production renderer 的准入标准。
- 增加 XML snapshot 测试。
- 继续保持默认 fail-closed。

目标：

- 没有真实设备时，先把 renderer 输出稳定性和边界管住。
- 真实设备到位后，只补 vendor mapping 和差异，不重做框架。

## 9. 当前可暂停点

今天可以在这里暂停，原因：

- 最新代码已经推送到 GitHub。
- 最新 CI 已绿色。
- 关键进度已文档化。
- 剩余工作已经按优先级拆清楚。
- 没有正在运行的必要本地任务。

下次继续时建议直接从 P0 缺口开始：

```text
real XML sample validation
  -> parser profile adaptation
  -> renderer hardening
```
